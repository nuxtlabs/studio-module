import { defineTheme } from 'pinceau'

export default defineTheme({
  color: {
    $schema: {
      title: 'My Theme Colors',
      tags: [
        '@studio-icon ph:color'
      ]
    },
    black: '#000000'
  }
})
